#include <stdio.h>

int main()
{
    int arr[5] = {2,4,6,8,10};
    int *ptr = arr;
    int i, sum = 0;

    for(i = 0; i < 5; i++)
    {
        sum += *ptr;
        ptr++;
    }

    printf("Sum = %d", sum);

    return 0;
}
Sample Output
Sum = 30
