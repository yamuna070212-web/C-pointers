#include <stdio.h>

int main()
{
    int arr[5] = {45, 22, 10, 55, 90};
    int *ptr = arr;
    int min = *ptr;

    for(int i = 1; i < 5; i++)
    {
        if(*(ptr + i) < min)
            min = *(ptr + i);
    }

    printf("Minimum = %d", min);

    return 0;
}
Sample Output
Minimum = 10
